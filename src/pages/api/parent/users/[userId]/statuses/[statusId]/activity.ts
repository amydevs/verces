import { type NextApiRequest, type NextApiResponse } from "next";

const createActivity = async (req: NextApiRequest, res: NextApiResponse) => {
    res.send("help")
}

export default createActivity;
